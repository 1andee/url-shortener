<!DOCTYPE html>
<html lang="en">
<head>
  <title>URL Shortener Service</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <style>
  .jumbotron {
    padding: 20px 20px;
  }
  </style>
</head>

<body>

<div class="jumbotron">
  <h1 class="display-3" >URL Shortening service</h1>
  <p class="lead">A full-stack web app to shorten long URLs.</p>

  <% if (!username) { %>
    <button type="button" class="btn btn-primary btn-lg form-inline">Register</button>
    <button type="button" class="btn btn-secondary btn-lg">Login</button>
  <% } %>
  <% if (username) { %>
    <form method="POST" action="/logout" class="form-inline">
      <a href="/urls" class="btn btn-primary btn-lg" role="button">
        My URLs
      </a>
      <a href="/urls/new" class="btn btn-primary btn-lg form-inline" role="button">
        Add new
      </a>
      <button type="submit" class="btn btn-primary btn-lg form-inline" value="Logout">
        Logout
      </button>
    </form>
    Welcome, <%= username %>
  <% } %>
</div>
